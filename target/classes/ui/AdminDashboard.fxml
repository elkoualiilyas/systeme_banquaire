<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<BorderPane xmlns="http://javafx.com/javafx" 
            xmlns:fx="http://javafx.com/fxml"
            fx:controller="ui.controller.AdminDashboardController"
            stylesheets="@../css/styles.css">

    <top>
        <ToolBar styleClass="toolbar">
            <Label text="💳 Banque Moderne" styleClass="title-label"/>
            <Pane HBox.hgrow="ALWAYS"/>
            <Button text="Exporter CSV" onAction="#exportClientsCsv" styleClass="secondary-button"/>
            <Button text="Comptes" onAction="#openComptes" styleClass="secondary-button"/>
            <Button text="Opérations" onAction="#openOperations" styleClass="primary-button"/>
            <Button text="Historique" onAction="#openHistorique" styleClass="secondary-button"/>
        </ToolBar>
    </top>

    <center>
        <VBox spacing="20" styleClass="vbox">
            <padding><Insets top="20" right="20" bottom="20" left="20"/></padding>
            
            <Label text="Gestion des Clients" styleClass="title-label"/>
            
            <!-- Formulaire création client -->
           <HBox spacing="10">
			    <TextField fx:id="cinField" promptText="CIN"/>
			    <TextField fx:id="nomField" promptText="Nom"/>
			    <TextField fx:id="prenomField" promptText="Prénom"/>
			    <Button text="➕ Ajouter" onAction="#creerClient" styleClass="btn-primary"/>
			    <Button text="✏️ Modifier" onAction="#modifierClient" styleClass="btn-secondary"/>
			    <Button text="🗑️ Supprimer" onAction="#supprimerClient" styleClass="btn-secondary"/>
		   </HBox>

            <!-- Tableau clients -->
            <Label text="Liste des clients" styleClass="label"/>
            <TableView fx:id="clientsTable" prefHeight="450">
                <columns>
                    <TableColumn fx:id="idColumn" text="ID" prefWidth="60"/>
                    <TableColumn fx:id="cinColumn" text="CIN" prefWidth="120"/>
                    <TableColumn fx:id="nomColumn" text="Nom" prefWidth="180"/>
                    <TableColumn fx:id="prenomColumn" text="Prénom" prefWidth="180"/>
                </columns>
            </TableView>
        </VBox>
    </center>

</BorderPane>
